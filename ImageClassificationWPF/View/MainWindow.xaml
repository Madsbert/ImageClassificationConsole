<Window x:Class="ImageClassificationWPF.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="70"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        
        <Image HorizontalAlignment="Left" 
               Height="310" Margin="0,10,0,0" Grid.Row="1" 
               VerticalAlignment="Top" Width="397"
               Source="{Binding CurrentPath.Name}"/>
        
        <TextBox HorizontalAlignment="Center" 
                 TextWrapping="Wrap" 
                 Text="{Binding CurrentPath.Name, UpdateSourceTrigger=PropertyChanged}"
                 VerticalAlignment="Center" Width="120"/>
        
        <Button Grid.Column="1" Content="Find Picture File" 
                HorizontalAlignment="Center" Margin="0,29,0,0" 
                VerticalAlignment="Top" Command="{Binding FindPictureCmd}"/>

        <Label Grid.Column="1" Content="Label" 
               HorizontalAlignment="Left" Margin="74,10,0,0" 
               Grid.Row="1" VerticalAlignment="Top" 
               Height="354" Width="276"/>
    </Grid>
</Window>
